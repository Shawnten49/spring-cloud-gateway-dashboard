# 保存即生效，配套操作审计

路由配置的保存语义为"保存即生效"：校验通过后写入真源并立即热刷新到网关，不做草稿/发布两阶段；v1 以操作审计日志弥补"没有草稿/回滚"带来的追溯缺口。

理由：用户的核心诉求是"动态修改、保存配置、不重启生效"，两阶段发布（草稿 → 发布）引入状态机和版本管理，留给 v2。审计日志记录操作者、时间、路由及变更前后内容，保证每次生效动作可追溯。

Status: accepted
