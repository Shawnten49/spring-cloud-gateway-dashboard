# 管理后台内嵌于网关进程

管理后台与网关运行在同一个 Spring Boot 进程内（单实例 MVP），而非独立的 Admin 服务。

理由：MVP 阶段一个进程、一个端口即可完成「查看 → 修改 → 保存 → 生效」的端到端演示；管理 API 按独立模块组织，未来拆分为独立服务时改动可控。

配套范围决策：本版本只管理路由（RouteDefinition），不管理全局过滤器、server 等完整网关配置；多实例刷新传播（消息总线/轮询）留待多实例版本。

Status: accepted
