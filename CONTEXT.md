# Gateway Dashboard

Spring Cloud Gateway 的管理后台：登录后可查看、修改、保存网关路由配置，改动无需重启网关即可生效。

## Language

**路由配置 (Route Configuration)**:
一条网关转发规则，包含目标地址（URI）、匹配条件（predicates）、过滤器链（filters）、执行顺序（order）和附加元数据（metadata）。
_Avoid_: 配置、规则（过于宽泛）

**真源 (Source of Truth)**:
路由配置唯一权威的持久化位置；当前为数据库，后台与网关读写同一个真源。
_Avoid_: 主库、配置源

**保存 (Save)**:
将路由配置写入真源并立即生效的动作；保存前必须通过路由校验，保存后网关无需重启。
_Avoid_: 更新（"更新"未表达生效语义）

**发布 (Publish)**:
（v2 概念）将草稿配置正式生效的动作。当前版本不区分草稿与生效，保存即发布，本词暂不启用。

**生效路由 (Effective Route)**:
当前已在网关中实际生效、参与转发匹配的路由。保存操作经热刷新后变为生效路由；"网关状态"页展示的就是生效路由，用于验证保存是否生效。
_Avoid_: 在线路由、运行中路由

**停用 (Disable)**:
将路由标记为不参与转发但保留配置的动作；停用的路由仍可查看、编辑和重新启用。
_Avoid_: 删除（删除会移除配置且不可恢复）

**路由校验 (Route Validation)**:
保存前对路由各字段（尤其是 predicates 与 filters）合法性进行的检查，防止错误配置上线。

**操作审计 (Audit Log)**:
对路由配置的每次保存、删除动作留下的记录：操作者、时间、目标路由、变更前后内容，用于追溯与回查。

**用户 (User)**:
可登录管理后台的账号，拥有 ADMIN 或 VIEWER 角色。
_Avoid_: 账号（账号是凭据载体，不是身份概念）

**角色 (Role)**:
权限集合。ADMIN 可查看并修改路由配置；VIEWER 只能查看。
_Avoid_: 权限（权限是更细粒度的概念，本轮不引入）

**网关实例 (Gateway Instance)**:
一个运行中的 Spring Cloud Gateway 进程。当前版本为单实例部署；真源落库的设计允许未来扩展到多实例。
