# 路由配置真源使用数据库

路由配置的真源选择数据库，而非网关 YAML 文件或 Nacos 等配置中心。后台与网关读写同一张路由表，改动保存后由网关内部热刷新使新路由生效。

理由：与「管理后台 + 保存」的产品定位最匹配；不依赖额外中间件，本地可自包含运行；网关重启后配置不丢失。实现上通过自定义路由定位器接入，保留日后切换到配置中心（Nacos）的抽象余地。

Status: accepted
